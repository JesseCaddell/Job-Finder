[build]
publish = "."
functions = "netlify/functions"

# Optional: run the feed puller automatically once a day.
# (You can also just hit "Pull feed now" in the app's Settings.)
[functions."fetch-jobs"]
schedule = "@daily"